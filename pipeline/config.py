# Fraction of total entries in a single cluster that triggers an imbalance warning.
IMBALANCE_THRESHOLD = 0.50

# Minimum percentage-point spread in per-bin theme share to be considered "dynamic".
# Themes varying less than this across all bins are treated as stable in the report.
STABILITY_BAND = 0.10
